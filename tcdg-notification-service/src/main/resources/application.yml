server:
  port: 8086

spring:
  datasource:
    url: jdbc:h2:mem:tcdg-notification-servicedb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
